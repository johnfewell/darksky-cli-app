//span[@class="skycon swip"] => Weather now icon
//span[@class="temp swip"] => Temp now number
//span[@class="summary swap"] => Text desc weather now one word
//span[@class="next swap"] => Longer now desc
//div[@id="nextHourClear"] => Next hour description
//span[@class="hour"] => Selects all hours
//div[@id="timeline"] => Selects all temps for the day, separate by degree symbol
//span[@class="name"] => Selects all days in 8 d forcast
//span[@class="minTemp"] => Returns each of the mintemps
//span[@class="maxTemp"] => Returns each of the maxtemps
//span[@class="skycon"] => Returns each of the sky icons
//div[@class="summary"] => Returns each of the 8 day summaries
